{
  "name": "@cmm/db",
  "version": "0.0.1",
  "private": true,
  "description": "Database abstraction layer for CMM",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint src/",
    "test": "vitest run"
  },
  "dependencies": {
    "@cmm/core": "workspace:*",
    "expo-sqlite": "^15.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "peerDependencies": {
    "react": "^18.0.0"
  }
}
